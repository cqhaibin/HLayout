//服务


class Service {
    /**
     * 添加一个容器或组件
     * @param opt 配置参数
     * type: 容器，组件
     * name: 名称
     * key: key，
     * code: 编号
     * @param parent 父级组件
     */
    add (opt, parent){
        let isRoot = parent ? false: true;
    }
}

export default Service;